<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <title>Vacancies</title>
    <link
      rel='stylesheet'
      href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css'
    />
  </head>
  <body>
    <nav class='navbar navbar-dark bg-dark px-4'>
      <a class='navbar-brand' href='/'>Job Portal</a>
      <div>
        <a href='/vacancies' class='text-white me-3'>Vacancies</a>
        <a href='/auth/login' class='btn btn-outline-light btn-sm'>Logout</a>
      </div>
    </nav>

    <div class='container mt-4'>
      <h4 class='mb-4'>All Vacancies</h4>
      <div class='row'>
        {{#each vacancies}}
          <div class='col-md-4 mb-3'>
            <div class='card shadow-sm'>
              <div class='card-body'>
                <h5 class='card-title'>{{title}}</h5>
                <p class='text-muted'>{{location}}</p>
                <p class='fw-bold'>${{salary}}</p>
                <a href='/vacancies/{{id}}' class='btn btn-primary btn-sm'>View
                  Details</a>
              </div>
            </div>
          </div>
        {{/each}}
      </div>
    </div>
  </body>
</html>